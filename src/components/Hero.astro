<section class="hero-system">
  <div class="container hero-grid">

    <div class="hero-content">

      <h1>
        Multi-Layer Packaging<br />
        Engineered as a System
      </h1>

      <p class="hero-subtitle">
        We develop complete packaging systems, combining flexible pouches, paper cartons, and retail display structures into a single coordinated framework.
      </p>

      <div class="hero-tags">
        <span>Flexible + Carton Integration</span>
        <span>Retail Optimization</span>
      </div>

      <div class="hero-actions">
        <a href="/packaging-system" class="btn-primary">
          Explore Packaging System
        </a>
        <a href="/knowledge/retail-ready-packaging-system" class="btn-secondary">
          Retail-Ready Guide
        </a>
      </div>

    </div>

    <div class="hero-visual">
      <div class="system-diagram-placeholder">
        <span>System Architecture Visual</span>
      </div>
    </div>

  </div>
</section>

<style>
.hero-system {
  padding: 80px 0 80px;
  background: linear-gradient(180deg, #f6f9fc 0%, #ffffff 70%);
  position: relative;
  overflow: hidden;
}

.hero-system::before {
  content: "";
  position: absolute;
  top: -250px;
  right: -250px;
  width: 700px;
  height: 700px;
  background: radial-gradient(circle, rgba(0,119,255,0.12), transparent 70%);
  filter: blur(100px);
  z-index: 0;
}

.hero-grid {
  display: grid;
  grid-template-columns: 1.1fr 1fr;
  gap: 80px;
  align-items: center;
  position: relative;
  z-index: 1;
}

.hero-eyebrow {
  font-size: 13px;
  text-transform: uppercase;
  letter-spacing: 1.5px;
  color: #666;
  font-weight: 500;
}

.hero-system h1 {
  font-size: 40px;
  line-height: 1.1;
  margin: 0px 0px 30px;
  font-weight: 700;
  color: #111;
}

.hero-subtitle {
  font-size: 17px;
  color: #555;
  line-height: 1.7;
  max-width: 560px;
  margin-bottom: 30px;
}

.hero-tags {
  display: flex;
  gap: 14px;
  flex-wrap: wrap;
  margin-bottom: 36px;
}

.hero-tags span {
  font-size: 13px;
  background: #eef4ff;
  padding: 8px 14px;
  border-radius: 20px;
  color: #005edb;
  font-weight: 500;
}

.hero-actions {
  display: flex;
  gap: 18px;
}

.hero-visual {
  display: flex;
  justify-content: center;
}

.system-diagram-placeholder {
  width: 100%;
  max-width: 480px;
  height: 420px;
  border-radius: 20px;
  background: linear-gradient(135deg, #e8f1ff, #f9fbff);
  display: flex;
  align-items: center;
  justify-content: center;
  font-weight: 500;
  color: #0077ff;
  box-shadow: 0 30px 60px rgba(0,0,0,0.08);
}

@media (max-width: 992px) {
  .hero-grid {
    grid-template-columns: 1fr;
    text-align: center;
  }

  .hero-system h1 {
    font-size: 40px;
  }

  .hero-actions {
    justify-content: center;
  }

  .hero-tags {
    justify-content: center;
  }

  .hero-visual {
    margin-top: 50px;
  }
}
</style>
